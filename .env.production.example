# Production frontend env
# Set your public API origin (HTTPS recommended), for example:
# VITE_API_BASE=https://api.your-domain.com
VITE_API_BASE=
# Optional direct fallback origin used only when API base is relative (/api).
VITE_DIRECT_API_FALLBACK=
