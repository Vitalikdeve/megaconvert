# @megaconvert/client-sdk

Typed browser-safe HTTP helpers used by the web app.

Contained here:
- resilient JSON client with timeout support and normalized error handling
- contract-aware system and health clients
- shared request parsing behavior for frontend data hooks
