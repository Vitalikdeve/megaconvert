# @megaconvert/design-system

Shared design tokens and reusable React primitives for the messenger web experience.

Contained here:
- theme tokens for light/dark runtime modes
- color, spacing, radius, shadow, and motion foundations
- reusable shell primitives such as buttons, surfaces, status badges, empty/error states, and skeletons
- global CSS variables that the app maps into Tailwind utilities
