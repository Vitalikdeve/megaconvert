# Local Docker Stack

`compose.local.yml` provides the shared local services used by the approved product blueprint:

- PostgreSQL
- Redis
- MinIO
- OpenSearch
- Mailpit
- LiveKit
- Coturn

Start the stack with `pnpm dev:infra`.
