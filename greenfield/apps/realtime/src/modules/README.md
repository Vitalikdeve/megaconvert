# Realtime HTTP Modules

This folder owns the realtime runtime's HTTP management surfaces.

- `health` and `system` are implemented now.
- transport-facing websocket namespaces live under `src/gateway`.
