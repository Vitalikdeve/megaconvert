# Realtime Gateway Modules

This folder owns websocket namespaces and connection-layer concerns.

- `system` is implemented now for transport verification.
- `presence` and `typing` are reserved for later realtime semantics.
