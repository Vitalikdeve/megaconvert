# Worker Modules

This folder owns the worker's runtime HTTP surfaces and will later host queue consumers.

- `health` and `system` are implemented now.
- `jobs` remains a folder-only boundary until async product work is introduced.
