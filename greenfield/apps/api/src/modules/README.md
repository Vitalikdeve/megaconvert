# API Modules

This folder is the long-term bounded-context home for the HTTP API.

- `health` and `system` are implemented now.
- `auth`, `users`, `messaging`, `meetings`, `media`, `notifications`, `search`, `settings`, and `security` are reserved and intentionally empty at this phase.
